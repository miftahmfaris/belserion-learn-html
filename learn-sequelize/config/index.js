const {
    PORT,
    DATABASE_HOST,
    DATABASE_PASSWORD,
    DATABASE_NAME,
    DATABASE_USER
} = require("./environment");

module.exports = {
    PORT,
    DATABASE_HOST,
    DATABASE_PASSWORD,
    DATABASE_NAME,
    DATABASE_USER
};
